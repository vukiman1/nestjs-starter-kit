version: '3.8'
services:
  api:
    build: .
    ports: ['3000:3000']
    env_file: .env
    # depends_on: [db]
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: nestdb
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: pass
